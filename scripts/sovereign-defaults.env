# Default values for SentinelSquad Board project fields.
# Override with MVP_STATUS, MVP_AGENT, MVP_PRODUCT, MVP_TYPE, MVP_PRIORITY when running the script.
# Used only when a field has no override and no current value on the board.
MVP_STATUS=Backlog
# Keep empty to avoid leaking demo/default agent assignments into board cards.
MVP_AGENT=
MVP_PRODUCT=amanoba
MVP_TYPE=Feature
MVP_PRIORITY=P0
